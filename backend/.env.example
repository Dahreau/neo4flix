# Copy this file to .env (same folder) before running `docker compose up`.
# .env is gitignored - real values live only on your machine, never in git.
# The values below are safe placeholders for local dev; change them for
# anything beyond your own machine.

NEO4J_PASSWORD=neo4flix_dev
JWT_SECRET=change-me-neo4flix-dev-secret-key-minimum-256-bits-long-please
